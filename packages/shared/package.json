{
  "name": "@rag/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types consumed by apps/web and apps/api. Types only — no runtime logic.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "echo \"(no lint configured for @rag/shared)\""
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
